---
description: Locked technology stack and exclusions
alwaysApply: true
---

# Locked stack

**Use:** Claude (stateless), Prisma, Supabase Postgres, Supabase Storage (via `StorageProvider` only), Stripe Connect, custom auth, Fastify API, Next.js web.

**Forbidden:** OpenAI, LangGraph, agent frameworks, Supabase Auth, Outlook calendar.

**AI:** Structured JSON output; app executes actions; confidence threshold 0.8; escalate below.

**API-first:** Business logic in `apps/api` only. Web uses typed API client + TanStack Query. No Prisma in web. No Server Actions as canonical writes.

**MVP channel:** SMS only (Twilio). WhatsApp and web widget are V2.

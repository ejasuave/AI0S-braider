/** Minimum touch target per visual-identity-and-ux.md (44×44px). */
export const MIN_TOUCH_TARGET_PX = 44;

/** Tailwind class matching min-h-11 (2.75rem = 44px at default scale). */
export const TOUCH_TARGET_CLASS = 'min-h-11';

/** Inline link styled for reliable mobile tap targets. */
export const TOUCH_LINK_CLASS =
  'inline-flex min-h-11 items-center px-1 text-sm font-medium text-primary underline-offset-2 hover:underline active:underline';
